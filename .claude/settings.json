{
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "cd \"$DIRENV_DIR\" && pre-commit run\n",
            "type": "command"
          }
        ],
        "matcher": "^(Edit|MultiEdit|Write)$"
      }
    ]
  }
}
