{
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "cd \"$(dirname $DIRENV_FILE)\" && pre-commit run\n",
            "type": "command"
          }
        ],
        "matcher": "^(Edit|MultiEdit|Write)$"
      }
    ]
  }
}
